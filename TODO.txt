TODO - TrayMe


- Show window if tray icon is missing, but is still hidden
- Command line traying program (ex: TrayMe.exe -t "Some Program.exe" -h "A" -e  ..  -t: tray window, -e: do not display errors, -h: hotkey)
  Name: TrayMeCmd.exe
  Waits for first window and trays it
- Workaround the TrackContextMenu() "feature"
- Nmumonics in the contet menu
- Multiple window traying
  - Window handle becomes a complex drop-down .. all items are trayed window handles
- TrayMe Hotkeys ??
  - Win+T - Tray/Untray current window
  - Window-specific hotkeys (assigned to a particular hotkey)
